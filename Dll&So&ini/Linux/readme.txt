Only Interbase 2020 update 6

Englis Version
.so to be downloaded into the executable folder through the project deployment
(for mysql see the container dockerfile in the "PAServer script update 12.1" folder)


Italian Version
.so da scaricare nella cartella dell'eseguibile attraverso il deploy del progetto
(per mysql vedi il dockerfile del container nella cartella "PAServer script update 12.1")